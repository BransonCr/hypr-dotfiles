# Your personal tweaks — edit freely (ported from your HyDE userprefs).

input {
    numlock_by_default = true
    accel_profile = flat  # disables mouse acceleration (flat = 1:1 sensitivity)
    touchpad {
        natural_scroll = no
    }
}

gestures {
    # workspace_swipe = true
}

decoration {
    inactive_opacity = 0.875
}

misc {
    # enable_swallow = true
    # swallow_regex = (kitty|foot|Alacritty)
}
